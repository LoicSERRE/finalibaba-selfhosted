# Test-only deps - never installed into the runtime image (Dockerfile only
# uses requirements.txt). CI installs both files to run the test suite.
pytest>=9.1.1
# Coverage report for the local SonarQube scan - see sonar-project.properties'
# sonar.python.coverage.reportPaths. Not needed to just run the tests.
pytest-cov>=7.1.0
